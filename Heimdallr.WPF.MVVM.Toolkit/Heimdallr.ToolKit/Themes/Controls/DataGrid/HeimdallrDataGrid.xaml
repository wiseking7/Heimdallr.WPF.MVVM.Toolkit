<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrls="clr-namespace:Heimdallr.ToolKit.UI.Controls"
                    xmlns:covnt="clr-namespace:Heimdallr.ToolKit.Converters">

  <!-- HeimdallrDataGrid 기본 스타일 -->
  <Style TargetType="{x:Type ctrls:HeimdallrDataGrid}"
         BasedOn="{StaticResource {x:Type DataGrid}}">
    <!-- 기본 동작 속성 -->
    <Setter Property="AutoGenerateColumns" Value="False"/>
    <Setter Property="IsReadOnly" Value="True"/>
    <Setter Property="RowHeaderWidth" Value="0"/>
    <Setter Property="Background" 
            Value="{Binding RelativeSource={RelativeSource Self}, Path=GridBackground}"/>
    <Setter Property="GridLinesVisibility" Value="Horizontal"/>
    <Setter Property="HorizontalGridLinesBrush" 
            Value="{Binding RelativeSource={RelativeSource Self}, 
                      Path=HorizontalGridLinesBrushCustom}"/>
    <Setter Property="Foreground" Value="#FFFFFFFF"/>
    <Setter Property="FontSize" 
            Value="{Binding RelativeSource={RelativeSource Self}, Path=DataGridFontSize}"/>

    <!-- 수정: 헤더/본문을 같은 Visual 트리 안에 둔 안전한 구조 -->
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:HeimdallrDataGrid}">

          <!-- DataGrid 전체를 감싸는 최외곽 Border -->
          <Border Background="{TemplateBinding Background}" 
                  BorderBrush="{TemplateBinding BorderBrush}" 
                  BorderThickness="{TemplateBinding BorderThickness}" 
                  Padding="{TemplateBinding Padding}" 
                  SnapsToDevicePixels="True">

            <!-- 스크롤 및 콘텐츠 표시를 담당하는 ScrollViewer -->
            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="false">

              <!-- ScrollViewer의 기본 템플릿 재정의 -->
              <ScrollViewer.Template>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">

                  <!-- 내부 레이아웃 Grid: 행과 열로 구성 -->
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <!-- 3개의 컬럼 정의: 좌측 버튼, 본문, 우측 스크롤바 -->
                      <ColumnDefinition Width="Auto"/>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- 3개의 행 정의: 상단 헤더, 본문, 하단 수평 스크롤바 -->
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                      <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- [좌상단] 전체 선택 버튼 DataGrid.SelectAllCommand에 바인딩된 버튼으로,
                         헤더 전체 보이기 설정에 따라 Visible/Collapsed 토글 -->
                    <Button Command="{x:Static DataGrid.SelectAllCommand}" 
                            Focusable="false" 
                            Style="{DynamicResource {ComponentResourceKey ResourceId=DataGridSelectAllButtonStyle, TypeInTargetAssembly={x:Type DataGrid}}}" 
                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.All}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" 
                            Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>

                    <!-- [상단 헤더 배경 영역]
                          헤더의 배경색을 DataGrid의 HeadColumnBackground 속성에 바인딩하여 적용 -->
                    <Border Grid.Column="1"  
                            Background="{Binding HeadColumnBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid}}">
                      <DataGridColumnHeadersPresenter 
                            x:Name="PART_ColumnHeadersPresenter"                
                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                    </Border>

                    <!-- [본문 내용 영역] 실제 아이템들을 렌더링하는 ScrollContentPresenter.
                         CanContentScroll 속성을 ScrollViewer에 맞게 전달 -->
                    <ScrollContentPresenter 
                            x:Name="PART_ScrollContentPresenter" 
                            CanContentScroll="{TemplateBinding CanContentScroll}" 
                            Grid.ColumnSpan="2" Grid.Row="1"/>

                    <!-- [우측 수직 스크롤바] 스크롤 가능한 높이(ScrollableHeight)에 맞게 스크롤 범위를 지정.
                          ScrollViewer의 VerticalOffset에 따라 위치 변경.
                          수직 스크롤바 가시성 조절 -->
                    <ScrollBar x:Name="PART_VerticalScrollBar" 
                               Grid.Column="2" 
                               Maximum="{TemplateBinding ScrollableHeight}" 
                               Orientation="Vertical" 
                               Grid.Row="1" 
                               Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" 
                               ViewportSize="{TemplateBinding ViewportHeight}" 
                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                    <!-- [하단 수평 스크롤바 영역을 포함하는 Grid] -->
                    <Grid Grid.Column="1" Grid.Row="2">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>

                      <!-- [하단 수평 스크롤바] 스크롤 가능한 너비(ScrollableWidth)에 맞게 스크롤 범위 지정.
                          ScrollViewer의 HorizontalOffset에 따라 위치 변경.
                          수평 스크롤바 가시성 조절 -->
                      <ScrollBar x:Name="PART_HorizontalScrollBar" 
                                 Grid.Column="1" 
                                 Maximum="{TemplateBinding ScrollableWidth}" 
                                 Orientation="Horizontal" 
                                 Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" 
                                 ViewportSize="{TemplateBinding ViewportWidth}" 
                                 Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                    </Grid>
                  </Grid>
                </ControlTemplate>
              </ScrollViewer.Template>

              <!-- 스크롤 가능한 항목을 렌더링하는 영역 -->
              <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
            </ScrollViewer>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>

    <!-- 컬럼 헤더 스타일 -->
    <Setter Property="ColumnHeaderStyle">
      <Setter.Value>
        <Style TargetType="DataGridColumnHeader">
          <Setter Property="FontWeight" Value="Bold"/>
          <Setter Property="FontSize" 
                  Value="{Binding RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid},
                            Path=HeaderFontSize}"/>
          <Setter Property="Foreground" Value="#FFFFFFFF"/>

          <Setter Property="Background"
                  Value="{x:Null}"/>
          <Setter Property="Padding" Value="8,4"/>
          <Setter Property="HorizontalContentAlignment" Value="Center"/>
          <Setter Property="VerticalContentAlignment" Value="Center"/>
          <Setter Property="BorderThickness" Value="0,0,0,1"/>
          <Setter Property="BorderBrush" Value="#FF94A3B8"/>
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="DataGridColumnHeader">
                <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        Padding="{TemplateBinding Padding}">
                  <ContentPresenter Content="{TemplateBinding Content}" 
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </Border>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </Setter.Value>
    </Setter>

    <!-- Row(행) 스타일 -->
    <Setter Property="RowStyle">
      <Setter.Value>
        <Style TargetType="DataGridRow">
          <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid}, 
                                                  Path=GridBackground}"/>
          <Setter Property="Foreground" Value="White"/>
          <Setter Property="FontSize" Value="{Binding RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid}, 
                                                Path=DataGridFontSize}"/>
          <Setter Property="Margin" Value="0 4"/>
          <!-- 최소 높이 설정 -->
          <Setter Property="MinHeight" Value="35"/>
          <Setter Property="Padding" Value="0"/>
          <Setter Property="HorizontalContentAlignment" Value="Center"/>
          <Setter Property="VerticalContentAlignment" Value="Center"/>

          <!-- 행 스타일에 필요한 Template 업데이트 -->
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="DataGridRow">
                <Border x:Name="DGR_Border" 
                        Background="{TemplateBinding Background}" 
                        BorderThickness="0"
                        SnapsToDevicePixels="True" 
                        Height="Auto" 
                        VerticalAlignment="Stretch">

                  <!-- 행의 내용을 포함하는 SelectiveScrollingGrid -->
                  <SelectiveScrollingGrid>
                    <SelectiveScrollingGrid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto"/>
                      <ColumnDefinition Width="*"/>
                    </SelectiveScrollingGrid.ColumnDefinitions>
                    <SelectiveScrollingGrid.RowDefinitions>
                      <RowDefinition Height="*"/>
                      <RowDefinition Height="Auto"/>
                    </SelectiveScrollingGrid.RowDefinitions>

                    <!-- 셀을 보여주는 CellsPresenter -->
                    <DataGridCellsPresenter Grid.Column="1" 
                                            ItemsPanel="{TemplateBinding ItemsPanel}" 
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                    <!-- 행 헤더는 숨김 처리 -->
                    <DataGridRowHeader Grid.Column="0" Visibility="Collapsed" />
                  </SelectiveScrollingGrid>
                </Border>

                <!-- 상태별 스타일 변경: 마우스 오버 시, 선택 시 배경 변경 -->
                <ControlTemplate.Triggers>
                  <!-- 마우스 오버 -->
                  <!-- 수정: 마우스 오버 시에도 Height와 VerticalAlignment 적용 -->
                  <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="DGR_Border" 
                            Property="Background" 
                            Value="{Binding RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid},
                                      Path=RowMouseOverBackground}"/>
                    <Setter Property="Foreground" Value="#FFF1D4D4"/>
                  </Trigger>

                  <!-- 선택 시 -->
                  <Trigger Property="IsSelected" Value="True">
                    <Setter TargetName="DGR_Border"
                            Property="Background" 
                            Value="{Binding RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid}, 
                                      Path=RowSelectedBackground}"/>
                    <Setter Property="Foreground" Value="#FFFFFFFF"/>
                  </Trigger>
                  <!-- 비활성화 상태 -->
                  <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#FF9CA3AF"/>
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </Setter.Value>
    </Setter>

    <!-- 셀 스타일 -->
    <Setter Property="CellStyle">
      <Setter.Value>
        <Style TargetType="DataGridCell">
          <Setter Property="Padding" Value="4,2"/>
          <Setter Property="HorizontalContentAlignment" Value="Center"/>
          <Setter Property="VerticalContentAlignment" Value="Center"/>
          <Setter Property="Foreground" Value="#FFFFFFFF"/>
          <Setter Property="BorderThickness" Value="0"/>
          <Setter Property="BorderBrush" Value="Transparent"/>

          <Style.Triggers>
            <!-- 수정: 마우스 오버 시 Foreground 변경 -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Foreground" Value="#FFFFF4B7"/>
            </Trigger>

            <!-- 선택 시에도 셀 스타일 변경 없음 -->
            <Trigger Property="IsSelected" Value="True">
              <!-- 선택 배경 제거 -->
              <Setter Property="Background" 
                      Value="{Binding RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrDataGrid}, 
                                Path=SelectedCellBackground}"/>
              <!-- 글자색 변경 -->
              <Setter Property="Foreground" Value="#FFFFFFFF"/>
              <!-- 선택 테두리 제거 -->
              <Setter Property="BorderThickness" Value="0"/>
            </Trigger>
          </Style.Triggers>
        </Style>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>